{
  "version": 4,
  "identityFields": ["projectId", "rootObjectiveId", "candidateGeneration", "mutationEpoch", "profileId", "catalogVersion", "fence"],
  "scenarios": [
    { "id": "qtb-profile-catalog", "kind": "positive", "expect": "verification-obligations/v1 exact mandatory catalogs for all runner profiles" },
    { "id": "qtb-schema-positive", "kind": "positive", "expect": "authority tables, versioned profile catalog, and foreign keys" },
    { "id": "qtb-ledger-duplicate", "kind": "negative", "expect": "identity-indexed duplicate action rejected" },
    { "id": "qtb-candidate-immutable", "kind": "negative", "expect": "materialized candidate cannot mutate" },
    { "id": "qtb-lease-binding", "kind": "negative", "expect": "lease candidate and identity tuple must match" },
    { "id": "qtb-one-current-fence", "kind": "negative", "expect": "one current unfenced lease per tuple" },
    { "id": "qtb-receipt-fence", "kind": "negative", "expect": "stale or mismatched receipt commit rejected" },
    { "id": "qtb-receipt-byte-mutation", "kind": "negative", "expect": "receipt bytes immutable" },
    { "id": "qtb-terminal-pair-byte-mutation", "kind": "negative", "expect": "terminal pair bytes immutable" },
    { "id": "qtb-pair-receipt", "kind": "negative", "expect": "terminal pair must reference same candidate receipt" },
    { "id": "qtb-bridge-transaction", "kind": "positive", "expect": "exact authoritative G0 mandatory catalog and five typed candidate-bound evidence rows commit under a live fenced lease" },
    { "id": "qtb-lease-committed", "kind": "positive", "expect": "successful bridge atomically marks matching lease committed" },
    { "id": "qtb-zero-obligation", "kind": "negative", "expect": "bridge rejects candidate with no mandatory obligations" },
    { "id": "qtb-missing-evidence", "kind": "negative", "expect": "bridge rejects missing mandatory evidence binding" },
    { "id": "qtb-wrong-obligation-id", "kind": "negative", "expect": "bridge rejects wrong mandatory catalog ID" },
    { "id": "qtb-omitted-obligation-id", "kind": "negative", "expect": "bridge rejects omitted mandatory catalog ID" },
    { "id": "qtb-extra-obligation-id", "kind": "negative", "expect": "bridge rejects extra mandatory catalog ID" },
    { "id": "qtb-duplicate-evidence", "kind": "negative", "expect": "bridge rejects non-one-to-one mandatory evidence IDs" },
    { "id": "qtb-backdated-bridge", "kind": "negative", "expect": "bridge timestamp cannot precede receipt or terminal pair" },
    { "id": "qtb-expired-bridge", "kind": "negative", "expect": "bridge rejects lease at expiry boundary" },
    { "id": "qtb-stale-fence-bridge", "kind": "negative", "expect": "bridge rejects a superseded lease fence" },
    { "id": "qtb-bridge-mismatch", "kind": "negative", "expect": "bridge foreign-key and byte identity mismatch rejected" },
    { "id": "qtb-bridge-failures-atomic", "kind": "positive", "expect": "failed bridge attempts leave lease uncommitted" },
    { "id": "qtb-idempotent-replay", "kind": "positive", "expect": "identical object bytes replay without overwrite" },
    { "id": "qtb-idempotent-mutation", "kind": "negative", "expect": "different replay bytes rejected" },
    { "id": "qtb-crash-before-bridge", "kind": "positive", "expect": "uncommitted bridge leaves no terminal pair transaction" },
    { "id": "qtb-catalog-exact-all-profiles", "kind": "positive", "expect": "every profile has the exact mandatory catalog with no omissions or extras" },
    { "id": "qtb-catalog-insert-immutable", "kind": "negative", "expect": "extra catalog rows are rejected for every seeded profile" },
    { "id": "qtb-catalog-update-immutable", "kind": "negative", "expect": "catalog row substitution or mandatory mutation is rejected for every seeded profile" },
    { "id": "qtb-catalog-delete-immutable", "kind": "negative", "expect": "catalog obligation omission is rejected for every seeded profile" },
    { "id": "qtb-catalog-wrong-identity", "kind": "negative", "expect": "wrong profile or catalog version cannot bind a lease" },
    { "id": "qtb-concurrent-initial-claim", "kind": "positive", "expect": "concurrent initial claims resolve to one winner" },
    { "id": "qtb-takeover-before-expiry", "kind": "negative", "expect": "takeover one tick before expiry is rejected" },
    { "id": "qtb-takeover-at-expiry", "kind": "positive", "expect": "expired incumbent can be recovered at equality" },
    { "id": "qtb-takeover-after-expiry", "kind": "positive", "expect": "expired incumbent can be recovered after expiry" },
    { "id": "qtb-recovery-winner", "kind": "positive", "expect": "only the higher fence recovery lease is current" },
    { "id": "qtb-stale-owner-commit", "kind": "negative", "expect": "stale owner and fence cannot commit" },
    { "id": "qtb-stale-lease-mutation", "kind": "negative", "expect": "superseded lease fence, expiry, and profile cannot mutate" },
    { "id": "qtb-lease-identity-mutation", "kind": "negative", "expect": "lease identity, binding, profile, catalog, fence, and timing columns cannot mutate" },
    { "id": "qtb-lease-direct-commit", "kind": "negative", "expect": "lease cannot commit without a matching authoritative bridge transaction" },
    { "id": "qtb-lease-committed-insert", "kind": "negative", "expect": "lease cannot be inserted already committed" },
    { "id": "qtb-lease-delete-immutable", "kind": "negative", "expect": "current and superseded lease rows cannot be deleted" },
    { "id": "qtb-superseded-lease-delete-immutable", "kind": "negative", "expect": "superseded lease deletion is rejected and historical fence remains" },
    { "id": "qtb-lease-committed-regression", "kind": "negative", "expect": "committed lease cannot regress to uncommitted" },
    { "id": "qtb-monotonic-fence", "kind": "negative", "expect": "fence and claim time cannot regress" },
    { "id": "qtb-reordered-events", "kind": "negative", "expect": "reordered source sequence cannot claim authority" }
  ]
}
